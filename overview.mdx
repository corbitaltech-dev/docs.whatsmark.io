---
title: "API Overview"
description: "Overview of the WhatsMark public REST API — authentication, scopes, pagination, and how to get access."
icon: "eye"
keywords: ["WhatsMark", "API", "REST API", "developer", "token", "scopes", "pagination"]
---

## What is the WhatsMark API

The WhatsMark REST API lets you interact with your workspace programmatically — contacts, templates, messages, conversations, channels, and OTP verification — instead of using the dashboard by hand. It's served from a dedicated host, `api.whatsmark.io`, separate from your tenant subdomain.

This page is the short version — what the API is, how to get a token, and the conventions every endpoint follows. **The complete, endpoint-by-endpoint reference lives at [api.whatsmark.io/docs](https://api.whatsmark.io/docs).**

<Card title="Full API reference — api.whatsmark.io/docs" icon="book-open" href="https://api.whatsmark.io/docs" horizontal>
  Interactive Swagger UI for the WhatsMark Tenant REST API (v1, OAS 3.1). Browse every endpoint grouped by module, expand one to see its parameters, request body, scopes, and example responses — then click **Authorize**, paste your bearer token, and run real requests straight from the browser.
</Card>

### What you'll find there

<CardGroup cols={2}>
  <Card title="Every endpoint, grouped by module" icon="list-tree">
    Root, Auth & Account, Channels, Contacts, Statuses, Sources, Groups, Labels, Templates, Messages, Conversations, and OTP — with a **Filter by tag** box to jump straight to the group you need.
  </Card>
  <Card title="Try it out, live" icon="play">
    Use the **Authorize** button to add `Authorization: Bearer <token>`, pick the `https://api.whatsmark.io` server, and send real requests against your workspace without leaving the page.
  </Card>
  <Card title="OpenAPI spec" icon="file-json">
    Download the raw OAS 3.1 document from the **OpenAPI Spec** button (`/openapi/whatsmark-api-v1.json`) to generate clients or import it into your own tooling.
  </Card>
  <Card title="Postman collection" icon="download">
    The **Postman Collection** button exports every request, with example payloads and responses, ready to import into Postman.
  </Card>
</CardGroup>

<Warning>
  **The API is not available on the Free plan.** REST API access is a **Professional** plan feature — Free-plan workspaces can use the full dashboard, but any request to `api.whatsmark.io` will be rejected until you upgrade. Reseller plans also include API access, with a higher (or unlimited) monthly request quota.
</Warning>

## Getting access

<Steps>
  <Step title="Upgrade to Professional">
    If you're on the Free plan, upgrade from **Billing** in the account menu (click your name at the bottom of the sidebar) first. See [Manage your plan and billing](/getting-started/overview#manage-your-plan-and-billing).
  </Step>
  <Step title="Create a workspace API token">
    Go to **Setup → API Keys** and create a token. Choose the abilities (scopes) it needs — tokens are only granted the scopes you select.
  </Step>
  <Step title="Authenticate your requests">
    Every request must include your token as a bearer token:

    ```text
    Authorization: Bearer YOUR_API_TOKEN
    ```
  </Step>
  <Step title="Call the API">
    Send requests to `https://api.whatsmark.io/api/v1/...`. Start with `GET /api/v1/ping` to confirm your token and abilities are working.
  </Step>
</Steps>

## Base URL

```text
https://api.whatsmark.io/api/v1
```

## Scopes (abilities)

Each endpoint requires a specific token ability (scope), for example `account:read` or `channels:read`. Create your token with the abilities that match the endpoints you plan to call — a request will be rejected with `403 Forbidden` if your token is missing the required scope.

## Pagination

List endpoints return a consistent envelope:

```json
{
  "data": [ ... ],
  "links": { "next": "...", "prev": "..." },
  "meta": { ... }
}
```

Large or append-only collections (contacts, group members, webhooks) use opaque cursor pagination — keep following `links.next` until it's `null`. `per_page` is clamped between 5 and 100.

## Async sends

Message and OTP send endpoints return `202 Accepted` immediately and are processed on a background queue rather than sent synchronously.

## Errors

Errors use a consistent envelope:

```json
{
  "message": "...",
  "error_code": "...",
  "status": 4xx
}
```

## Modules

The API covers: **Auth & Account** · **Channels** · **Contacts** · **Contact Statuses** · **Contact Sources** · **Contact Groups** · **Contact Labels** · **Templates** · **Messages** · **Conversations** · **OTP**.

Each module is documented endpoint by endpoint — parameters, request bodies, required scopes, and example responses — in the reference at [api.whatsmark.io/docs](https://api.whatsmark.io/docs).

<Card title="Open the API reference" icon="book-open" href="https://api.whatsmark.io/docs" horizontal>
  Browse all endpoints, try them live, and grab the OpenAPI spec or Postman collection from the buttons at the top of the page.
</Card>

<Info>
  Already have a Professional plan? Head to **Settings → API Keys** in your workspace to create your first token.
</Info>