---
api: "GET /api/v1/templates/{template_id}"
playground: "simple"
title: "Get Template"
description: "Retrieve a single WhatsApp template by ID."
---

<Info>
  **Scope required:** `templates:read`
</Info>

Retrieve a single template by its identifier.

<ParamField path="template_id" type="string" required>
  Identifier of the template.
</ParamField>

<ResponseField name="data" type="object">
  <Expandable title="properties">
             

    <ResponseField name="components" type="object[]">
      <Expandable title="properties">
         
      </Expandable>
    </ResponseField>

    <ResponseField name="example_values" type="object">
      <Expandable title="properties">
      </Expandable>
    </ResponseField>

       
  </Expandable>
</ResponseField>

<RequestExample>

```bash Request
curl --request GET \
  --url https://api.whatsmark.io/api/v1/templates/1 \
  --header "Authorization: Bearer YOUR_API_TOKEN"
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "data": {
    "id": 1,
    "channel_id": 1,
    "platform": "whatsapp",
    "platform_template_id": "123456789",
    "name": "order_confirmation",
    "language": "en_US",
    "category": "utility",
    "sub_type": null,
    "status": "APPROVED",
    "quality_rating": "GREEN",
    "components": [
      { "type": "BODY", "text": "Your order {{1}} is confirmed." }
    ],
    "example_values": { "body": ["#A123"] },
    "rejected_reason": null,
    "last_synced_at": "2026-06-26T09:00:00+00:00",
    "created_at": "2026-06-20T10:00:00+00:00",
    "updated_at": "2026-06-26T09:00:00+00:00"
  }
}
```

</ResponseExample>

Common error responses: `401` (missing/invalid token), `402` (no valid subscription), `403` (missing `templates:read` scope), `404` (template not found), `429` (monthly API request quota exhausted).