---
api: "GET /api/v1/sources"
playground: "simple"
title: "List Sources"
description: "All contact sources, ordered by sort_order."
---

<Info>
  **Scope required:** `sources:read`
</Info>

All contact sources, ordered by `sort_order`.

<ResponseField name="data" type="object[]">
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      The source's unique identifier.
    </ResponseField>

    <ResponseField name="name" type="string">
      The source's name.
    </ResponseField>

    <ResponseField name="color" type="string">
      The source's color.
    </ResponseField>

    <ResponseField name="sort_order" type="integer">
      The source's sort order.
    </ResponseField>

    <ResponseField name="is_system" type="boolean">
      Whether this is a system-defined source.
    </ResponseField>

    <ResponseField name="created_at" type="string">
      When the source was created.
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash Request
curl --request GET \
  --url https://api.whatsmark.io/api/v1/sources \
  --header "Authorization: Bearer YOUR_API_TOKEN"
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "data": [
    {
      "id": 1,
      "name": "Website",
      "color": "#00AA55",
      "sort_order": 1,
      "is_system": false,
      "created_at": "2026-06-26T10:00:00+00:00"
    }
  ]
}
```

</ResponseExample>

Errors: `401` missing/invalid/expired token, `402` no valid subscription or payment under review, `403` token missing the `sources:read` scope, `429` monthly API request quota exhausted.