---
title: "Contact Configuration"
description: "Set up labels, groups, statuses, sources, and custom fields before you start adding contacts to WhatsMark, plus auto-create defaults for inbound conversations."
icon: "address-book"
keywords: ["WhatsMark", "contacts", "labels", "groups", "statuses", "sources", "custom fields", "auto-create"]
---

Before you start adding contacts in bulk, it's worth setting up the building blocks you'll use to organize them. These live under **Contacts → Settings** as five separate tabs.

<Tip>
You don't have to do this before adding your first contact — but setting these up early means every contact you create afterward can be organized correctly from the start.
</Tip>

## Labels

Freeform tags you can attach to any contact (e.g. "VIP", "Needs follow-up"). Create, edit, delete, and drag to reorder — the order here is the order they appear in throughout the product.

## Groups

Named collections of contacts, used for manual segmentation and as an audience option when creating a [Campaign](/messaging/campaigns). Create, edit, and delete groups; add or remove members from the Contacts page itself.

## Statuses

Represents where a contact is in your lifecycle (e.g. "New", "Contacted", "Customer"). Create, edit, delete, and reorder statuses, and mark one as the **default** status assigned to new contacts.

## Sources

Tracks where a contact came from (e.g. "Website", "Referral", "Ad Campaign"). Create, edit, delete, and reorder — useful for the dashboard's contact-source breakdown chart.

## Custom Fields

Add your own fields to the contact record beyond the built-in ones. Each custom field has:

- A **type**: text, dropdown, radio, checkbox, switch, or date.
- Type-specific **options/settings** (e.g. the list of choices for a dropdown).
- An **active/inactive** toggle — inactive fields are hidden from forms but keep their stored data.
- Drag-to-reorder, same as labels/statuses/sources.

<Note>
Custom fields you create here immediately become available as merge-field tokens (see [Merge Fields](/messaging/merge-fields)) and as mapping targets when importing contacts or mapping [WhatsApp Flow submissions](/automation/whatsapp-flows).
</Note>

## Contact auto-create defaults

When someone messages your WhatsApp number for the first time and no matching contact exists, WhatsMark can create one automatically. Configure how under **Settings → Contacts**:

<Steps>
<Step title="Set global defaults">
  Choose the default **lifecycle stage**, **status**, **source**, **labels**, and **groups** applied to auto-created contacts, plus an **assignment strategy** (e.g. leave unassigned, or assign to a specific team member).
</Step>

<Step title="Override per WhatsApp channel (optional)">
  If you have multiple WhatsApp numbers connected, you can override these defaults for a specific channel — for example, giving contacts from a support number a different default status than contacts from a sales number.
</Step>
</Steps>

<Tip>
Removing a channel-level override falls that channel back to using the global defaults automatically.
</Tip>

## What's next

<CardGroup cols={2}>
  <Card title="Managing Contacts" icon="address-book" href="/contacts/managing-contacts">
    Start adding and organizing contacts using the labels, groups, statuses, and fields you just configured.
  </Card>

  <Card title="Team & Roles" icon="users" href="/core-setup/team-and-roles">
    Invite your team so contacts can be assigned to the right person.
  </Card>
</CardGroup>
