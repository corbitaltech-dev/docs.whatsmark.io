---
title: "API Tokens"
description: "Create API tokens so other apps can connect to your workspace."
icon: "key"
keywords: ["WhatsMark", "API tokens", "developer", "REST API"]
---

import { BrowserFrame } from '/snippets/browser-frame.jsx';

An API token lets another app connect to your workspace. After you create a token, that app can use it to access your data and perform actions based on the permissions you allow.

Find it under **Setup → API Tokens**.

<Note>
The API is only available on a paid plan.
</Note>

## Creating a token

<Steps>
<Step title="Click Create Token">
  Enter a name so you can identify the token later, such as Website or Zapier.
</Step>

<Step title="Choose what it can do">
  Select what the token is allowed to do. Only choose the permissions the app needs.

  ![The Create API Token form with a name field and grouped scope checkboxes for account, contacts and messaging](/core-setup/img/api-token-form.png)
</Step>

<Step title="Set an expiry date (optional)">
  Choose when the token should expire, or leave it empty so it never expires.
</Step>

<Step title="Copy the token">
  The token is shown only once after it is created. Copy it and save it in a safe place because you cannot view it again.
</Step>
</Steps>

## Your tokens

The list shows each token's **name**, **permissions**, **last used date**, **expiry date**, and **created date**. If a token never expires, it shows Never. You can also search by token name.

<BrowserFrame
  src="/core-setup/img/api-token.png"
  url="whatsmark.io/api-keys"
  alt="The API Tokens page listing tokens with their name, scopes, last used, expiry and created date, plus API Docs, Postman Collection and Create Token buttons"
  caption="Image shows the API Tokens page."
/>

At the top of the page, you'll find two helpful buttons:

- **API Docs** — View the complete API documentation.
- **Postman Collection** — Download ready-to-use API requests for Postman.

## Regenerate or revoke

Open a token's `⋮` menu for its two actions:

- **Regenerate** — Create a new secret for the same token, Update the new token in your app because the old one stops working.
- **Revoke** — Permanently deletes the token. Any app using it will stop working immediately.

<Warning>
You cannot change a token's name or permissions after creating it. If you need different permissions, create a new token and revoke the old one.
</Warning>

## Using your token

Send the token with every request, in the `Authorization` header:

```text
Authorization: Bearer YOUR_API_TOKEN
```

For example:

```bash
curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  https://api.whatsmark.io/api/v1/contacts
```


## What's next

<CardGroup cols={2}>
  <Card title="API Overview" icon="book" href="/overview">
    The base URL, pagination, and every available endpoint.
  </Card>

  <Card title="Webhooks" icon="webhook" href="/settings/webhooks">
    Get events sent to your own system as they happen.
  </Card>
</CardGroup>
